# 行动指南

## 1. 当前项目状态

当前工程主体已经搭好，核心思路为：

```text
整数规划 + 目标规划
```

当前工程结构已经包括：

```text
common/
parts/
scripts/
extensions/
outputs/
```

其中：

- `common/` 存放建模和通用内容；
- `parts/` 存放四种机械臂情形的独立入口；
- `scripts/` 存放批量运行、对比或 CSV 生成脚本；
- `extensions/` 存放非线性规划扩展；
- `outputs/` 存放运行结果。

目前主要缺少三类内容：

1. 2B/3B 优化算法主体；
2. 二臂/三臂对比分析部分；
3. 非线性规划扩展主体。

下面按照任务说明具体应该去哪个文件夹、改什么文件、建什么文件。

---

## 2. 任务一：完成 2B/3B 优化算法主体（fal、xjq）

### 2.1 当前位置

当前优化算法占位文件是：

```text
common/solver_optimized.py
```

现在这个文件只是返回占位结果：

```text
PLACEHOLDER_NOT_SOLVED
```

它没有真正求解任务调度问题。

### 2.2 要做什么

需要在 `common/solver_optimized.py` 中实现真正的优化方法。

函数入口必须保留：

```python
def solve_schedule(instance: Instance, modes_by_task: Dict[int, List[Mode]]) -> dict:
```

不要改函数名，也不要改参数格式。因为以下入口都依赖这个函数：

```text
parts/03_optimized_two_arm/run.py
parts/04_optimized_three_arm/run.py
scripts/run_four_case_framework.py
```

### 2.3 输出格式必须保持一致

`solve_schedule()` 最终必须返回一个字典，至少包含：

```python
{
    "status": "...",
    "algorithm_name": "...",
    "model_name": "...",
    "cmax": ...,
    "total_energy": ...,
    "load_imbalance": ...,
    "arm_loads": {...},
    "goal_programming": {...},
    "schedule": [...],
    "tasks": [...],
    "arms": {...},
}
```

其中最关键的是：

```text
cmax
total_energy
load_imbalance
arm_loads
schedule
```

如果这些字段缺失，后续 CSV、画图、调度表输出都会出问题。

### 2.4 可以采用的优化方法方向

优化方法不要再简单调用 `common/solver_basic.py` 中的求解器，否则和基础方法没有区别。

可以考虑以下方向之一：

#### 方向 A：规则构造 + 局部交换

文件位置：

```text
common/solver_optimized.py
```

大致流程：

```text
1. 按任务类型、距离或目标盒分配任务；
2. 给每只机械臂生成初始任务序列；
3. 计算 Cmax 和总能耗；
4. 尝试交换两个任务；
5. 如果交换后 Cmax 更小，或者 Cmax 不变但能耗更小，则接受；
6. 重复若干次。
```

#### 方向 B：小规模隐枚举 / 剪枝搜索

文件位置：

```text
common/solver_optimized.py
```

大致流程：

```text
1. 枚举每个任务的可行执行模式；
2. 对明显不可能优于当前解的分支提前剪枝；
3. 保留当前最优的 Cmax、能耗和负载差；
4. 最终返回最优或近似最优结果。
```

#### 方向 C：先分配、后排序的分阶段优化

文件位置：

```text
common/solver_optimized.py
```

大致流程：

```text
1. 先按距离或负载规则把任务分配给机械臂；
2. 再对每只机械臂内部任务排序；
3. 最后计算 Cmax、能耗和负载差；
4. 通过局部调整改进结果。
```

### 2.5 建议新增的辅助文件

如果 `solver_optimized.py` 太长，可以在 `common/` 下新增：

```text
common/optimized_heuristics.py
```

用于存放：

```text
任务分配规则；
局部交换函数；
Cmax 和能耗计算函数；
负载均衡计算函数。
```

然后在 `solver_optimized.py` 中调用。

---


## 4. 任务三：完成二臂/三臂对比分析（wml）

### 4.1 当前已有输入

当前脚本：

```text
scripts/run_four_case_framework.py
```

已经可以生成：

```text
outputs/four_case_framework/optimized_2B3B_time_energy.csv
```

该 CSV 只包含：

```text
counts_code
n1
n2
n3
n4
total_tasks
seed
status_2B_optimized
cmax_2B_optimized
energy_2B_optimized
status_3B_optimized
cmax_3B_optimized
energy_3B_optimized
```

它是后续对比分析的输入，不负责下结论。

### 4.2 建议新增文件

建议新增：

```text
scripts/analyze_2B3B_comparison.py
```

这个脚本专门读取：

```text
outputs/four_case_framework/optimized_2B3B_time_energy.csv
```

然后生成分析结果。



## 5. 任务四：完成非线性规划扩展（xjq 谁有空谁帮我）

### 5.1 当前位置

当前非线性规划占位文件是：

```text
extensions/nonlinear_programming/continuous_motion_nlp_placeholder.py
```

当前输出位置已经建立：

```text
outputs/nonlinear_programming/
```

### 5.2 要做什么

非线性规划不建议强行并入主体整数规划模型，而是作为扩展问题。

推荐问题定义为：

```text
在调度序列已经确定后，对机械臂连续运动速度和能耗进行优化。
```

### 5.3 建议新增文件

可以新增：

```text
extensions/nonlinear_programming/continuous_motion_model.py
extensions/nonlinear_programming/continuous_motion_solver.py
scripts/run_nonlinear_extension.py
```

其中：

| 文件 | 作用 |
|---|---|
| `continuous_motion_model.py` | 定义非线性规划变量、目标函数和约束 |
| `continuous_motion_solver.py` | 实现求解方法，或者先保留理论求解接口 |
| `run_nonlinear_extension.py` | 独立运行非线性规划扩展 |

### 5.4 非线性规划输入从哪里来

建议从基础方法或优化方法的 `result_*.json` 中读取调度结果。

例如读取：

```text
outputs/01_basic_two_arm/results/result_*.json
outputs/02_basic_three_arm/results/result_*.json
```

或者后续读取四情形汇总脚本生成的结果。

从调度结果中提取：

```text
任务顺序；
机械臂编号；
每段运动距离；
每段允许时间；
当前总能耗。
```

### 5.5 推荐非线性规划模型

可以设每段运动速度为连续变量：

```text
v_k
```

目标函数：

```text
min sum(alpha * d_k + beta * d_k * v_k^2)
```

约束：

```text
sum(d_k / v_k) <= T_allowed
v_min <= v_k <= v_max
```

其中：

```text
d_k：第 k 段运动距离；
v_k：第 k 段运动速度；
T_allowed：调度层给定的最大允许时间。
```

该模型中包含：

```text
v_k^2
d_k / v_k
```

因此可以作为非线性规划扩展内容。

### 5.6 推荐输出

输出位置：

```text
outputs/nonlinear_programming/
```

推荐输出文件：

```text
nonlinear_input_segments.json
nonlinear_result.json
speed_profile.csv
energy_comparison.csv
nonlinear_energy_curve.png
```

---

